[project]
name = "polyamg-ml"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "torch>=2.2",
  "onnx>=1.15",
  "pyyaml>=6.0",
  "matplotlib>=3.8",
  "onnxscript>=0.7.0",
]
